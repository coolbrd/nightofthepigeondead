// find the player
player = instance_find(obj_player, 0);

// the color that the bar fills up with
bar_color = c_red;
// the base color of the bar
bar_background = c_white;

// the amount of the bar that's filled
bar_value = 0;
// the total of the bar that can be filled
bar_max = 100;